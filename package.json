{
	"private": true,
	"scripts": {
		"dev": "nuxi dev",
		"build": "nuxi build",
		"build:clean": "npm run clean && npm ci && npm run build",
		"generate": "nuxi generate",
		"preview": "nuxi preview",
		"postinstall": "nuxi prepare",
		"start": "node .output/server/index.mjs",
		"clean": "rm -fr .output node_modules .nuxt",
		"predeploy": "npm run build:clean && cp .deploy/firebase.json .output && cp .deploy/.env .output/server",
		"deploy": "firebase deploy -c .output/firebase.json --project ffa-website-41f93 --only hosting,functions"
	},
	"devDependencies": {
		"@nuxt/types": "^2.17.2",
		"@types/node": "^18",
		"@types/serve-static": "^1.15.1",
		"firebase-functions": "^4.5.0",
		"firebase-functions-test": "^3.1.0",
		"nuxt": "^3.8.1",
		"sass": "^1.49.7",
		"sass-loader": "^12.4.0"
	},
	"dependencies": {
		"@zafir.co.ao/lightray": "^1.0.0-beta.2",
		"antbox-sdk": "^1.1.0-beta.7",
		"jose": "^4.8.3"
	}
}
